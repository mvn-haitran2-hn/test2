# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: ubuntu-latest

# steps:
# - task: NodeTool@0
#   inputs:
#     versionSpec: '10.x'
#   displayName: 'Install Node.js'

# # - script: |
# #     npm install
# #     npm run build
# #   displayName: 'npm install and build'

# # - publish: $(System.DefaultWorkingDirectory)/kmaa-cms/dist
# #   artifact: kmaa-cms
# - publish: $(System.DefaultWorkingDirectory)/test2/test
#   artifact: kmaa-cms-test

jobs:
  # - job: Build
  #   displayName: Build
  #   steps:
  #     - task: NodeTool@0
  #       inputs:
  #         versionSpec: '10.x'
  #       displayName: 'Install Node.js'

  #   # - script: |
  #   #     npm install
  #   #     npm run build
  #   #   displayName: 'npm install and build'

  #   # - publish: $(System.DefaultWorkingDirectory)/kmaa-cms/dist
  #   #   artifact: kmaa-cms
  #     - publish: $(System.DefaultWorkingDirectory)/test2/test
  #       artifact: kmaa-cms-test
  - job: Build
    displayName: Build
    workspace:
      clean: outputs
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '18.x'
      displayName: 'Install Node.js'
    - script: |
        ls -l
        pwd
    - publish: $(System.DefaultWorkingDirectory)/test
      artifact: kmaa-cms-test
  - deployment: DeployWeb
    displayName: deploy Web App
    # creates an environment if it doesn't exist
    environment:
      name: 'Testenv'
      resourceType: VirtualMachine
      tags: tag1
    dependsOn: Build
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: kmaa-cms-test
          - script: |
              ls -l
              pwd
              hostname
              echo $(Pipeline.Workspace)
              echo "12345678911111111111111"
          - script: sudo cp -r $(**Pipeline.Workspace**)/kmaa-cms-test /var/www/kmaa-cms