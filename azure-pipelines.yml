# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

# pool:
#   vmImage: ubuntu-latest

# steps:
# - task: NodeTool@0
#   inputs:
#     versionSpec: '10.x'
#   displayName: 'Install Node.js'

# # - script: |
# #     npm install
# #     npm run build
# #   displayName: 'npm install and build'

# # - publish: $(System.DefaultWorkingDirectory)/kmaa-cms/dist
# #   artifact: kmaa-cms
# - publish: $(System.DefaultWorkingDirectory)/test2/test
#   artifact: kmaa-cms-test

jobs:
  - deployment: DeployWeb
    displayName: deploy Web App
    # creates an environment if it doesn't exist
    environment:
      name: 'Testenv'
      resourceType: VirtualMachine
      tags: tag1
    strategy:
      runOnce:
        deploy:
          steps:
          - script: |
              ls -l
              pwd
              hostname
          - download: current
            artifact: kmaa-cms-test
          - script: sudo cp -r kmaa-cms-test /var/www/kmaa-cms